const BigBtn = (props) => {
  return (
    <button className="border-semigreen-300 text-semigreen-300 rounded-full w-40 h-11 font-semibold">
      {props.title}
    </button>
  );
};

export default BigBtn;
